# Plan: Backend Core Setup

Establish FastAPI backend, google-adk loop setup, and LiteLLM OpenRouter connection wrapper.

## Proposed Changes

### Backend Core

#### [NEW] [requirements.txt](file:///Users/aloke/Repositories/AgentUI/backend/requirements.txt)
- Create requirements file with dependencies: `google-adk`, `litellm`, `fastapi`, `uvicorn`, `pyyaml`, `httpx`.

#### [NEW] [shared.py](file:///Users/aloke/Repositories/AgentUI/backend/shared.py)
- Setup LiteLLM OpenRouter key configuration.
- Accept OpenRouter free preset model (`openrouter/free`).

#### [NEW] [app.py](file:///Users/aloke/Repositories/AgentUI/backend/app.py)
- Create core FastAPI server routing.
- Expose a simple `/api/health` and skeleton `/api/chat` POST endpoint.

## Verification Plan

### Automated Tests
- Test server startup and query `/api/health` using `curl`.
