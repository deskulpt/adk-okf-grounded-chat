# Plan: Client-Side Session Knowledge & MarkItDown Conversion

Build frontend upload sidebar catalog explorer and integrate backend markitdown conversion endpoint.

## Proposed Changes

### Backend Component

#### [MODIFY] [backend/requirements.txt](file:///Users/aloke/Repositories/AgentUI/backend/requirements.txt)
- Add `markitdown>=0.0.1` and `python-multipart>=0.0.9`.

#### [MODIFY] [backend/app.py](file:///Users/aloke/Repositories/AgentUI/backend/app.py)
- Create `/api/concepts` endpoint.
- Create `/api/convert` endpoint using `markitdown`.
- Update `/api/chat` route to support dynamic `local_concepts` list.

### Frontend Component

#### [MODIFY] [frontend/src/components/Chat.tsx](file:///Users/aloke/Repositories/AgentUI/frontend/src/components/Chat.tsx)
- Reorganize the component layout into a side-by-side flexbox structure.
- Add Left Sidebar panel containing catalog listings, drag-and-drop uploader box, and a prominent privacy notice.
- Call POST `/api/convert` when files are uploaded to populate `localConcepts` React state.
- Include the `localConcepts` list inside the `/api/chat` fetch request body payload.

## Verification Plan

### Automated Tests
- Build frontend:
  ```bash
  cd frontend && npm run build
  ```
- Trigger file conversion endpoints via curl:
  ```bash
  curl -X POST -F "file=@test.txt" http://localhost:8040/api/convert
  ```

### Manual Verification
- Verify drag and drop uploader parses DOCX/PDF/TXT.
- Test queries match in-memory concepts.
